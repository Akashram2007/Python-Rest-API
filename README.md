# Flask REST API – User Management

This project is a simple *REST API built using Flask*.  
It demonstrates the basic CRUD operations: *GET, POST, PUT, DELETE* for managing user data.

## 🚀 Features
- Get all users  
- Get a single user by ID  
- Add a new user  
- Update an existing user  
- Delete a user  
- In-memory data storage (dictionary)  

## 🛠 Technologies Used
- Python
- Flask
- Postman / Curl for testing
